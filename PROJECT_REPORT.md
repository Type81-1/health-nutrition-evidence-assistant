# 赛道二：健康营养证据智能助手项目报告

## 1. 项目背景

本项目参考 OpenEvidence 这类证据检索型医疗 AI 工具的产品思路，完成一个面向健康营养场景的可演示 MVP。OpenEvidence 类产品的核心价值并不是让 AI 直接给出诊疗结论，而是帮助用户围绕具体问题快速找到可信、可追溯、可核查的证据，并基于证据生成带引用的回答。

在健康营养场景中，普通消费者经常会遇到“某种饮食是否真的有用”“某种保健品能不能替代治疗”“网上健康建议是否可信”等问题。如果直接让通用大模型回答，容易出现结论泛化、引用不明、边界不清甚至假引用的问题。因此，本项目选择构建一个小型证据增强问答系统，强调“先检索证据，再生成回答”。

## 2. 赛道选择

本项目选择 `赛道二：健康营养助手`。

赛道二的目标用户是普通消费者，关注主题包括血脂、高血压、饮食干预、营养健康和保健品边界。系统需要把专业文献、公开指南、系统综述和随机对照试验转化为普通人能理解的健康科普回答，同时保留证据来源和安全边界。

本项目当前聚焦以下问题：

- 地中海饮食对心血管风险有什么证据？
- 限钠饮食对高血压是否真的有帮助？
- 血脂偏高的人应该只靠保健品吗？
- DASH 饮食适合高血压人群吗？
- 鱼油能不能替代降脂药？
- 膳食纤维对 LDL 胆固醇有帮助吗？
- 控糖饮食是否一定要完全不吃主食？
- 含糖饮料和糖尿病风险有什么关系？
- 超加工食品对心血管健康有什么影响？

## 3. 项目目标

本项目的目标是完成一个可运行、可演示、可评估的健康营养证据助手 MVP。

核心目标包括：

- 用户可以输入健康营养相关问题。
- 系统先从本地证据库中检索相关证据。
- 回答中的关键结论带有 `[E1] [E2]` 形式的引用。
- 右侧展示证据来源、年份、证据类型、证据等级和摘录。
- 支持 PubMed 实时检索，补充最新论文线索。
- 明确提示系统仅用于健康科普，不替代医生诊疗。
- 使用固定测试集评估检索质量、引用完整性和安全提示。

## 4. 系统架构

系统采用轻量级 Web 应用结构：

```text
浏览器前端
  |
  |  /api/answer
  v
FastAPI 后端
  |
  |-- EvidenceStore：本地证据检索
  |-- AnswerService：证据约束回答生成
  |-- PubMedClient：PubMed 实时检索
  |-- OpenAICompatibleLlm：可选大模型增强
```

主要文件如下：

| 文件 | 作用 |
| --- | --- |
| `app/main.py` | FastAPI 服务入口 |
| `app/services/evidence_store.py` | 本地证据库、关键词检索、可选 Chroma 检索 |
| `app/services/answer_service.py` | 带引用的健康科普回答生成 |
| `app/services/pubmed_client.py` | PubMed E-utilities 实时检索 |
| `app/static/` | 前端页面 |
| `data/seed_evidence.json` | 本地种子证据库 |
| `data/evaluation_questions.json` | 固定评测问题 |
| `scripts/run_evaluation.py` | 评测脚本 |
| `tests/test_answer_service.py` | 单元测试 |

## 5. 数据来源

本项目的数据来源分为两类。

### 5.1 本地证据库

本地证据库位于：

```text
data/seed_evidence.json
```

当前包含 17 条健康营养相关证据，覆盖：

- 地中海饮食
- AHA 膳食指导
- WHO 钠摄入指南
- DASH 和 DASH-Sodium 试验
- Cochrane 减盐系统综述
- ESC 心血管预防指南
- 可溶性膳食纤维与 LDL 胆固醇
- 全谷物与血脂
- 植物甾醇与 LDL 胆固醇
- omega-3 / 鱼油与血脂管理
- WHO 糖摄入指南
- 含糖饮料与糖尿病风险
- 低 GI 饮食与糖尿病
- ADA 糖尿病营养治疗共识
- 超加工食品与心血管风险

证据类型包括：

```text
专业指南
国际指南
系统综述
Meta 分析
随机对照试验
科学声明/共识报告
```

### 5.2 PubMed 实时检索

系统通过 NCBI PubMed E-utilities 实现实时检索。

流程包括：

```text
中文问题 -> 英文 PubMed 检索词 -> esearch -> efetch -> 页面展示
```

如果 `efetch` 超时，系统会回退到 `esummary`，至少返回题名、期刊、年份和 PMID 链接。

## 6. 检索方法

### 6.1 本地关键词检索

为了保证课堂演示稳定，系统默认使用本地关键词检索，不依赖外部模型下载。

检索过程包括：

- 中文问题切分为 2-4 字短语。
- 加入营养健康领域同义词扩展。
- 标题命中给予更高权重。
- 专业指南、系统综述、随机对照试验等证据类型给予加权。
- 按得分排序，选取前 4 条证据。

例如，问题：

```text
限钠饮食对高血压是否真的有帮助？
```

会扩展出：

```text
限钠、减盐、低盐、食盐、钠、钠摄入、降低钠、高血压、血压、降压
```

因此系统会优先返回 DASH-Sodium 试验、WHO 钠摄入指南和 Cochrane 减盐系统综述。

### 6.2 可选 Chroma 语义检索

项目保留 Chroma 语义检索能力，但默认关闭，避免启动时下载模型影响演示稳定性。

如需启用，可在 `.env` 中设置：

```text
ENABLE_CHROMA=true
```

如果 Chroma 初始化失败，系统会自动回退到本地关键词检索。

## 7. 回答生成方法

系统回答分为两种模式：

### 7.1 默认模板回答

在不配置大模型 API 的情况下，系统使用确定性模板生成回答。

回答结构固定为：

```text
先说结论
证据怎么说
怎么把它做得更可执行
证据边界
安全提示
```

这种方式的优点是稳定、可控、不容易编造引用，适合 MVP 演示。

### 7.2 可选 LLM 增强回答

项目支持 OpenAI 兼容的大模型接口。如果配置以下环境变量，系统会尝试调用大模型生成更自然的回答：

```text
LLM_API_URL
LLM_API_KEY
LLM_MODEL
```

为了防止假引用，系统会校验模型输出中的引用编号。模型只能引用当前检索到的 `[E1] [E2]...`。如果模型没有引用，或引用了不存在的编号，系统会自动回退到模板回答。

## 8. 前端展示

前端页面支持：

- 输入自然语言问题。
- 点击示例问题快速提问。
- 展示回答正文和引用标记。
- 右侧展示来源卡片。
- 显示当前检索状态，例如：

```text
已从 local keyword retrieval 选取 4 条证据。
```

- 点击“实时检索 PubMed”获取最新论文线索。

页面强调“证据优先”，并在回答下方展示健康安全提示。

## 9. 评测设计

项目使用固定问题集进行基础评测。

评测文件：

```text
data/evaluation_questions.json
```

评测脚本：

```text
scripts/run_evaluation.py
```

评测指标包括：

| 指标 | 含义 |
| --- | --- |
| 引用完整性 | 回答是否包含 `[E1]` |
| 关键内容检查 | 回答是否包含预期关键词 |
| 来源命中 | 检索结果是否包含预期证据 ID |
| 安全提示 | 是否包含不替代诊疗的边界 |

当前评测集包含 10 个问题，覆盖高血压、血脂、保健品、DASH、膳食纤维、控糖、含糖饮料和超加工食品。

## 10. 评测结果

当前运行结果：

```text
[1] PASS 地中海饮食对心血管风险有什么证据？
[2] PASS 限钠饮食对高血压是否真的有帮助？
[3] PASS 血脂偏高的人应该只靠保健品吗？
[4] PASS DASH 饮食适合高血压人群吗？
[5] PASS 鱼油能不能替代降脂药？
[6] PASS 膳食纤维对 LDL 胆固醇有帮助吗？
[7] PASS 控糖饮食是否一定要完全不吃主食？
[8] PASS 含糖饮料和糖尿病风险有什么关系？
[9] PASS 超加工食品对心血管健康有什么影响？
[10] PASS 体检发现胆固醇高，生活方式能做什么？

通过 10/10。
```

单元测试结果：

```text
6 passed
```

这说明当前系统在固定评测集上能够检索到预期证据，并生成包含引用和安全边界的回答。

## 11. 示例演示

### 11.1 限钠饮食与高血压

问题：

```text
限钠饮食对高血压是否真的有帮助？
```

系统优先返回：

- DASH-Sodium 随机对照试验
- WHO 钠摄入指南
- Cochrane 减盐系统综述

回答会说明限钠和 DASH 饮食有助于降低血压，但应作为长期生活方式管理，不应替代医生建议的治疗。

### 11.2 鱼油与降脂药

问题：

```text
鱼油能不能替代降脂药？
```

系统优先返回：

- AHA omega-3 与高甘油三酯科学建议
- omega-3 补充剂与心血管风险 Meta 分析

回答会说明普通鱼油保健品不能等同于处方级 omega-3，也不能替代医生给出的降脂方案。

### 11.3 控糖饮食与主食

问题：

```text
控糖饮食是否一定要完全不吃主食？
```

系统优先返回：

- 低 GI / 低 GL 饮食系统综述
- ADA 糖尿病营养治疗共识

回答会说明控糖重点是改善碳水质量和整体饮食结构，而不是简单地完全不吃主食。

## 12. 安全、伦理与隐私边界

本项目定位为健康教育和文献线索整理工具，不用于真实诊疗。

系统明确提示：

```text
这是健康科普，不替代个体化诊疗。
正在使用降压药、降脂药、利尿剂，或处于孕期、有肾病/心衰等情况时，请先与医生或注册营养师讨论饮食调整。
```

项目不处理真实患者隐私信息，不要求用户输入姓名、病历号、检查报告或个人身份信息。

对于涉及用药、慢病、孕期、肾病、心衰等情况的问题，系统只提供一般健康科普信息，并提示咨询医生或注册营养师。

## 13. 当前局限

当前项目仍有以下局限：

- 本地证据库规模较小，只覆盖部分健康营养主题。
- 默认关键词检索无法完全替代专业语义检索和人工证据筛选。
- PubMed 实时检索只展示论文线索，不会自动判断研究质量。
- 模板回答较稳定，但语言灵活性有限。
- 未完成与通用大模型的系统性对比评估。
- 没有人工专家审核，不能用于真实医疗决策。

## 14. 后续优化方向

后续可以从以下方向继续改进：

- 扩展本地证据库到 30-50 条高质量指南、系统综述和随机对照试验。
- 增加证据等级筛选，例如只看指南、系统综述或随机对照试验。
- 增加 RAG vs 通用大模型对比评估，观察假引用、引用真实性和安全提示差异。
- 接入大模型 API，提升回答自然度，同时继续保留引用白名单校验。
- 将评测结果自动导出为 Markdown 或 JSON 报告。
- 增加“PubMed 查询词展示”，让用户看到系统如何把中文问题转为英文检索。
- 增加文档导入后的证据管理页面。

## 15. 总结

本项目完成了赛道二健康营养助手的核心 MVP：用户可以围绕高血压、血脂、控糖、保健品等常见健康问题提问，系统先检索本地证据，再生成带引用的中文科普回答，并可实时检索 PubMed 补充论文线索。

与直接让通用大模型回答相比，本项目更强调证据来源、引用可追溯、回答边界和安全提示。虽然当前系统仍然是教学原型，但已经跑通了 OpenEvidence 风格应用中最关键的闭环：

```text
问题 -> 证据 -> 带引用回答 -> 实时文献检索 -> 评估
```

当前评测结果为 `10/10`，说明系统在固定测试集上能够稳定检索到相关证据，并生成符合赛道二要求的可演示回答。
