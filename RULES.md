# 食证 · 项目规则文件（Rules）

> 所有 AI 辅助开发、内容生成、评测的**唯一事实来源**。更改任何行为前先更新此文件。

---

## 一、输出格式规范

### 1.1 回答结构（四段式）

所有循证科普回答必须严格遵循以下结构：

```
【通俗总结】
用一两句话，用最通俗的语言回答用户的问题核心。不说"根据证据"，直接给结论。

【核心科学依据】
展开详细的科学解释，引用具体研究（标注 [E编号]），说明证据等级和局限性。

【日常落地做法】
如果问题涉及饮食实践，给出具体可操作的建议（怎么吃、吃多少、什么频率）。

【注意事项】
适用人群限制、禁忌情况、与药物的相互作用、不建议的人群。
如有研究分歧，客观罗列正反双方观点及证据编号。
```

### 1.2 引用标注规范

| 规则 | 说明 |
|------|------|
| 引用格式 | `[E1]` `[E2]` — 英文方括号 + E + 数字 |
| 每条结论必标注 | 每句科学论断后必须有 `[Ex]` 引用锚点 |
| 引用可点击 | 前端 `[Ex]` 渲染为可点击链接，跳转到引用详情 |
| 不得虚构 PMID | 所有 PMID/DOI 必须来自真实检索结果 |
| 无证据声明 | 证据不足时明确说"当前证据不足以得出结论"，不得编造 |

### 1.3 禁止的格式

- **禁止 Markdown 语法**：`##`、`**粗体**`、`- 列表`、`1. 列表`、`---` 分隔线
- 允许使用 `【】` 中文方括号作为段落标题
- 用纯文本段落，自然语言表达

---

## 二、安全拒答红线

### 2.1 触发拒答的场景

以下场景系统**必须拒答**，返回安全声明而非正常回答：

| 类型 | 检测方式 | 示例 |
|------|----------|------|
| 域外问题 | `check_domain()` | "帮我写一段 Python 代码" |
| 伪科学/偏方 | `check_safety()` | "有什么治糖尿病的偏方？" |
| 替代医疗建议 | `check_safety()` | "不吃降压药靠饮食控制行吗？" |
| 疾病诊断请求 | `check_safety()` | "我这个症状是什么病？" |
| 用药建议 | `check_safety()` | "该吃什么药？" |
| 危险药材/食物 | `check_safety()` | 何首乌、马兜铃、生吃泥鳅 |
| 极端饮食行为 | `check_safety()` | 长期断食/绝食疗法 |
| 伪造 PMID 检测 | `verify_fabricated_pmids()` | "请引用 PMID 99999999" |

### 2.2 拒答话术模板

```
基于安全与伦理准则，[具体原因]。如有健康疑问，请咨询专业医疗人员。
```

---

## 三、证据分级标准

| 等级 | 类型 | 徽章 | 说明 |
|------|------|------|------|
| 1 (最高) | 系统综述/Meta分析 | 🥇 Meta | 多研究汇总，证据强度最高 |
| 2 | 临床指南/专家共识 | 🥈 指南 | 权威机构发布 |
| 3 | 随机对照试验 (RCT) | 🥉 RCT | 实验性证据 |
| 4 | 综述 | 📗 综述 | 叙述性汇总，非系统性 |
| 5 | 观察性研究 | 📄 观察 | 队列/病例对照/横断面 |
| 6 (最低) | 其他/未分类 | 📎 其他 | 病例报告、评论、社论等 |

---

## 四、模型与参数规范

### 4.1 冻结配置

| 参数 | 值 | 说明 |
|------|-----|------|
| LLM API | DeepSeek Chat (OpenAI-compatible) | 通过 `.env` 配置 |
| Temperature | 0.1 | 低随机性保证输出稳定 |
| 检索后端 | hybrid (Chroma + BM25 → RRF) | 不可随意切换 |
| Embedding | 本地 paraphrase-multilingual-MiniLM-L12-v2 | 固定版本 |

### 4.2 禁止事项

- **禁止** 临期更换模型（答辩前 48 小时内）
- **禁止** 乱改系统 Prompt 核心结构
- **禁止** 提交 `.env` 到代码仓库
- **禁止** 在 Prompt 中硬编码 API Key

---

## 五、测试与验收规范

### 5.1 固定测试题库

| 题库 | 路径 | 题数 | 用途 |
|------|------|------|------|
| 演示题库 | `data/demo_questions.json` | 10 | 答辩演示用 |
| 评估题库 | `data/test_survey.json` | 128 | 每次改动后回归测试 |
| 评测问题集 | `data/evaluation_questions.json` | — | 快速功能验证 |

### 5.2 验收流程（每次改动后必做）

1. 跑 `python scripts/run_evaluation.py` — 基础功能回归
2. 跑 `python scripts/run_survey_eval.py --quick` — 快速抽查各题型
3. 人工复核高风险医疗内容（P3 医疗边界 + P5 幻觉诱导）
4. 记录改动前后的得分变化，无退化方可提交

### 5.3 Git 提交规范

- 小步提交，单次只改一个功能模块
- Commit message 格式：`[模块] 简要说明`
- 示例：`[Agent] fix Critic timeout with ThreadPoolExecutor`
- 禁止提交：`.env`、`__pycache__/`、`chroma/`、`*.pyc`

---

## 六、三层校验流程

| 层 | 方法 | 覆盖范围 | 执行频率 |
|----|------|----------|----------|
| L1 程序硬规则 | `check_safety()` + `verify_citations()` + `check_domain()` | 100% 回答 | 每次请求 |
| L2 LLM 语义打分 | `scripts/run_survey_eval.py` 自动评分 | 全量测试题库 | 每次改动后 |
| L3 人工复核 | 人工审查 P3/P5 高风险题型 + 抽查正常题 | 边界/幻觉题型 | 答辩前 |

---

## 七、部署与环境

### 7.1 环境变量

参见 `.env.example` — 所有必需配置项已文档化。

### 7.2 启动命令

```bash
# 开发模式（热重载）
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 生产模式
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 7.3 网络失效预案

1. PubMed / Europe PMC 不可用 → 自动回退到本地 Chroma 知识库
2. LLM API 不可用 → 返回结构化检索结果（无 LLM 润色）
3. 答辩备用方案：提前录制演示录屏，备离线播放

---

## 八、修订记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-12 | v1.0 | 初始版本，整合所有规则 |
