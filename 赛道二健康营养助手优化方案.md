# 赛道二健康营养证据助手优化方案

## 1. 项目定位

本项目适合用于 `OpenEvidence 风格的证据智能助手 MVP` 中的赛道二：健康营养助手。

赛道二的目标是面向普通消费者，围绕血脂、高血压、饮食干预、营养健康等主题，把专业文献、公开指南和系统综述转化为可理解、可追溯、有安全边界的健康科普回答。

当前项目已经跑通核心闭环：

```text
用户问题 -> 本地证据检索 -> 带引用回答 -> PubMed 实时检索 -> 基础评估
```

因此后续优化重点不是重新选题，而是提升证据质量、检索效果、回答可信度、评估完整性和演示表达。

## 2. 当前项目已完成内容

### 2.1 已有功能

- 本地证据库：`data/seed_evidence.json`
- 本地检索：关键词检索，支持中文短语和营养领域同义词扩展
- 可选 Chroma 语义检索：模型下载成功后自动启用，失败时回退到关键词检索
- 带引用回答：回答中使用 `[E1] [E2]` 标注证据来源
- 来源展示：右侧展示标题、年份、证据类型、证据等级和摘录
- PubMed 实时检索：通过 NCBI E-utilities 获取最新论文线索
- 中文问题转英文检索词：提高 PubMed 查询成功率
- 健康安全提示：明确说明不替代诊疗
- 基础评估脚本：`scripts/run_evaluation.py`
- 单元测试：`tests/test_answer_service.py`

### 2.2 已覆盖的问题

当前项目主要覆盖以下健康营养主题：

- 地中海饮食与心血管风险
- 限钠饮食与高血压
- 血脂偏高与保健品

这些问题与赛道二要求高度一致。

## 3. 优化目标

优化目标可以概括为五个方向：

```text
证据更多
检索更准
回答更自然
评估更完整
演示更清楚
```

优先级建议如下：

| 优先级 | 优化方向 | 价值 |
| --- | --- | --- |
| P0 | 增加证据数据和测试问题 | 直接提升项目完整度 |
| P0 | 完善项目报告和演示路径 | 满足课程交付要求 |
| P1 | 优化检索和 PubMed 查询词 | 提升回答相关性 |
| P1 | 接入可选 LLM API | 让回答更自然、更像真实助手 |
| P2 | 增加 RAG vs 通用大模型对比 | 提升展示深度 |

## 4. 数据与证据库优化

### 4.1 扩充本地证据

当前 `data/seed_evidence.json` 只有少量种子证据。建议扩展到 15-30 条，覆盖更多赛道二常见问题。

建议新增主题：

- 高血压饮食管理：DASH、限钠、钾摄入、体重管理
- 血脂管理：饱和脂肪、反式脂肪、膳食纤维、坚果、鱼油
- 糖尿病饮食：低 GI 饮食、含糖饮料、体重控制
- 保健品边界：鱼油、红曲、维生素 D、植物甾醇
- 体重管理：能量摄入、超加工食品、饮食模式

每条证据建议包含：

```json
{
  "id": "唯一ID",
  "title": "文献或指南标题",
  "source_type": "指南/系统综述/随机对照试验/队列研究",
  "year": "年份",
  "url": "来源链接",
  "evidence_level": "证据等级",
  "content": "适合展示和引用的中文摘要"
}
```

### 4.2 优先选择高可信证据

证据优先级建议：

```text
临床指南 > 系统综述/Meta 分析 > 随机对照试验 > 大型队列研究 > 单个观察性研究
```

对于普通消费者健康科普，应尽量避免只引用单个小样本研究来支持强结论。

### 4.3 导入自己的资料

如果有 PDF 或 Markdown 资料，可以使用项目已有脚本导入：

```zsh
python scripts/ingest_markdown.py \
  --pdf "资料.pdf" \
  --title "资料标题" \
  --source-url "https://example.org"
```

或：

```zsh
python scripts/ingest_markdown.py \
  --markdown "资料.md" \
  --title "资料标题" \
  --source-url "https://example.org"
```

建议优先导入公开指南、综述或权威机构材料。

## 5. 检索优化

### 5.1 本地检索优化

当前关键词检索已经支持中文短语和领域同义词扩展。后续可以继续优化：

- 增加更多同义词，例如“低盐”“控盐”“钠摄入”“收缩压”“胆固醇”“甘油三酯”
- 对证据类型加权，优先返回指南、系统综述和随机对照试验
- 对标题命中加更高权重
- 避免主题漂移，例如限钠问题不应优先返回地中海饮食研究

可以把检索评分设计成：

```text
总分 = 问题关键词命中分 + 同义词命中分 + 标题命中加权 + 证据等级加权
```

### 5.2 PubMed 检索优化

PubMed 更适合英文医学关键词。当前项目已经把常见中文问题转换成英文检索词。

后续可以继续扩展：

| 中文主题 | PubMed 查询词 |
| --- | --- |
| 限钠/高血压 | dietary sodium reduction hypertension blood pressure |
| 地中海饮食/心血管 | Mediterranean diet cardiovascular disease prevention |
| 血脂/保健品 | dyslipidemia cholesterol dietary supplements cardiovascular risk |
| DASH 饮食 | DASH diet hypertension randomized trial |
| 膳食纤维/血脂 | dietary fiber LDL cholesterol meta-analysis |

如果要提升 PubMed 稳定性，可以配置 NCBI API key：

```zsh
export NCBI_EMAIL="your_email@example.com"
export NCBI_API_KEY="your_ncbi_api_key"
```

NCBI API key 主要提升访问稳定性和频率限制，不会自动提升回答质量。

## 6. 回答生成优化

### 6.1 当前回答方式

当前项目即使没有大模型 API，也可以用模板生成回答。模板回答的优点是稳定、可控、不容易编造引用；缺点是表达较固定，灵活性有限。

当前回答结构是：

```text
先说结论
证据怎么说
怎么把它做得更可执行
证据边界
安全提示
```

这个结构适合赛道二，因为它面向普通消费者，既要讲清楚，也要避免过度医疗建议。

### 6.2 接入 LLM API 的价值

如果配置大模型 API，可以让回答：

- 更贴合用户问题
- 语言更自然
- 能综合多条证据
- 能更清楚地区分“已知证据”和“证据不足”

可选环境变量：

```zsh
export LLM_API_URL="OpenAI兼容的 chat/completions 地址"
export LLM_API_KEY="your_api_key"
export LLM_MODEL="model_name"
```

不过 LLM API 不是必须。为了防止假引用，项目已经做了引用白名单校验：模型输出必须只引用当前检索到的 `[E1] [E2]...`，否则回退到模板回答。

### 6.3 建议优化 Prompt

Prompt 应继续强调：

- 只能使用给定证据
- 每个关键结论必须带引用
- 证据不足时明确说明
- 不提供诊断或用药调整建议
- 用普通消费者能理解的中文回答

## 7. 评估优化

### 7.1 扩大测试集

当前评估问题较少。建议扩展到 10-15 个问题。

建议问题：

```text
1. 限钠饮食对高血压是否真的有帮助？
2. 地中海饮食对心血管风险有什么证据？
3. 血脂偏高的人应该只靠保健品吗？
4. DASH 饮食适合高血压人群吗？
5. 鱼油能不能替代降脂药？
6. 膳食纤维对 LDL 胆固醇有帮助吗？
7. 高血压患者是不是越少吃盐越好？
8. 控糖饮食是否一定要完全不吃主食？
9. 体检发现胆固醇高，生活方式能做什么？
10. 保健品广告说能清血管，应该相信吗？
```

### 7.2 增加评估指标

可以从以下维度评价：

| 指标 | 含义 |
| --- | --- |
| 引用完整性 | 回答是否包含 `[E1]` 等引用 |
| 引用真实性 | 引用是否来自检索结果 |
| 相关性 | 来源是否与问题主题匹配 |
| 安全边界 | 是否提示不替代诊疗 |
| 拒答能力 | 证据不足时是否避免编造 |
| 可读性 | 是否适合普通消费者理解 |

### 7.3 对比实验

如果时间允许，可以增加一个小型对比：

```text
同一批问题：
1. 直接问通用大模型
2. 使用本项目 RAG 系统回答

比较：
- 是否有真实引用
- 是否出现假文献
- 是否有安全提示
- 是否能说明证据边界
```

这个对比能很好呼应项目背景中“专用医疗 AI vs 通用大模型”的讨论。

## 8. 前端与演示优化

### 8.1 页面可以增加的信息

建议在页面中增加：

- 当前检索后端：`local keyword retrieval` 或 `Chroma semantic retrieval`
- 当前回答使用模板还是 LLM
- PubMed 查询词提示
- 证据等级筛选或标签

### 8.2 演示路径

推荐演示顺序：

```text
1. 打开首页，说明这是赛道二健康营养助手
2. 点击“限钠饮食与高血压”
3. 展示回答中的 [E1] [E2] 引用
4. 展示右侧来源卡片和证据等级
5. 点击“实时检索 PubMed”
6. 展示最新 PubMed 论文
7. 强调安全提示：不替代医生诊疗
8. 展示评估脚本结果：通过 3/3 或扩展后的结果
```

### 8.3 展示时可以强调的亮点

- 不是直接让 AI 编答案，而是先检索证据
- 每条关键结论都有来源
- PubMed 可实时补充最新论文线索
- 对普通消费者使用中文科普表达
- 有医疗健康边界，避免诊疗建议

## 9. 项目报告建议结构

最终报告可以按以下结构写：

```text
1. 项目背景
2. 赛道选择：健康营养助手
3. 用户场景和核心问题
4. 系统架构
5. 数据来源
6. 检索与回答流程
7. PubMed 实时检索
8. 测试问题与评估结果
9. 安全、伦理与隐私边界
10. 当前局限
11. 后续改进方向
```

## 10. 三天实践优化计划

### 第 1 天：跑通和补数据

- 确认本地环境可运行
- 跑通 3 个示例问题
- 扩充 `data/seed_evidence.json`
- 增加 5-10 个测试问题

### 第 2 天：优化检索和回答

- 优化中文关键词和同义词
- 优化 PubMed 查询词映射
- 增加引用相关性检查
- 可选接入 LLM API
- 跑固定评估集

### 第 3 天：演示和报告

- 整理项目报告
- 准备 15-20 分钟演示
- 展示系统流程和评估结果
- 总结局限和后续方向

## 11. 当前项目最推荐的下一步

建议下一步按这个顺序推进：

```text
1. 扩展 data/seed_evidence.json 到 15 条左右
2. 扩展 data/evaluation_questions.json 到 10 条左右
3. 跑 scripts/run_evaluation.py 并记录结果
4. 写 PROJECT_REPORT.md
5. 准备演示脚本和截图
```

如果时间有限，最优先完成第 2、3、4 步，因为它们最直接对应课程交付物。
